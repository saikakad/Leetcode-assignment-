class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        vector<int> result;
        int count = 0;
        for(int i = 0; i < nums.size(); i++){
            count += nums[i];
            result.push_back(count);
            

        }
     return result;  
    }

};
